# Profitelligence MCP Server Environment Variables
# Copy this file to .env and fill in your actual values

# ===== Authentication Configuration =====
# Choose authentication method: 'api_key' or 'oauth'
PROF_AUTH_METHOD=api_key

# ===== Option 1: API Key Authentication (Simple, works with stdio/http) =====
# Generate an API key from https://profitelligence.com/account/api-keys
PROF_API_KEY=pk_your_api_key_here

# ===== Option 2: OAuth 2.1 (For Claude Desktop - automatic browser login) =====
# Claude Desktop will automatically discover OAuth support and trigger browser login
# No credentials or tokens needed - OAuth handles everything!
# PROF_AUTH_METHOD=oauth
# PROF_MCP_MODE=http  # OAuth requires HTTP mode (not stdio)
# PROF_MCP_PORT=3000  # Port for HTTP server
# Note: OAuth automatically uses Firebase Auth (no additional config needed)

# ===== Legacy: Manual Firebase JWT (Phase 1 - backward compatibility) =====
# Manual token copy/paste method (tokens expire after 1 hour)
# Only use this if you can't use OAuth for some reason
# PROF_AUTH_METHOD=firebase_jwt
# PROF_FIREBASE_ID_TOKEN=your_firebase_id_token_here

# ===== Optional Configuration =====

# API Base URL (production or development)
# Production: https://apollo.profitelligence.com
# Development: https://icarus.profitelligence.com
PROF_API_BASE_URL=https://apollo.profitelligence.com

# MCP Server Mode (stdio or http)
PROF_MCP_MODE=stdio

# HTTP Server Port (only used when PROF_MCP_MODE=http)
PROF_MCP_PORT=3000

# Log Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
PROF_LOG_LEVEL=INFO

# ===== OAuth Provider Configuration (for self-hosted instances) =====
# Only needed if running your own MCP server with OAuth login

# Google OAuth Client ID
# PROF_OAUTH_CLIENT_ID=your_google_oauth_client_id

# Google OAuth Client Secret
# PROF_OAUTH_CLIENT_SECRET=your_google_oauth_client_secret

# Firebase Web API Key (for Firebase Auth integration)
# PROF_FIREBASE_WEB_API_KEY=your_firebase_web_api_key

# Path to client config JSON (alternative to individual settings)
# PROF_OAUTH_CLIENT_CONFIG_PATH=./client-config.json

# ===== Advanced Configuration =====

# Request timeout in seconds
PROF_API_TIMEOUT=30

# Number of retry attempts for failed requests
PROF_API_RETRY_ATTEMPTS=3

# Enable response caching
PROF_CACHE_ENABLED=true

# Cache TTL in seconds
PROF_CACHE_TTL=300
